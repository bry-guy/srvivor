apiVersion: v1
kind: Service
metadata:
  name: castaway-discord-bot
  labels:
    app.kubernetes.io/name: castaway-discord-bot
    app.kubernetes.io/part-of: castaway
spec:
  selector:
    app.kubernetes.io/name: castaway-discord-bot
  ports:
    - name: http
      port: 8080
      targetPort: http
